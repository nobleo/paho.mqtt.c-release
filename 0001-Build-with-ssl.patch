From 6d361a3ac323f530f1f76b32e83e626c40eb4940 Mon Sep 17 00:00:00 2001
From: Tim Clephas <tim.clephas@nobleo.nl>
Date: Mon, 23 Aug 2021 09:54:43 +0200
Subject: [PATCH] Build with ssl

---
 debian/rules | 3 +++
 1 file changed, 3 insertions(+)

diff --git a/debian/rules b/debian/rules
index 18bea63..123c24e 100755
--- a/debian/rules
+++ b/debian/rules
@@ -21,6 +21,9 @@ export DEB_CXXFLAGS_MAINT_APPEND=-DNDEBUG
 # Solve shlibdeps errors in REP136 packages that use GNUInstallDirs:
 export DEB_HOST_MULTIARCH := $(shell dpkg-architecture -qDEB_HOST_MULTIARCH)
 
+# Build with ssl
+export DEB_CFLAGS_MAINT_APPEND = -DPAHO_WITH_SSL=ON -DPAHO_ENABLE_TESTING=OFF
+
 %:
 	dh $@ -v --buildsystem=cmake
 
-- 
2.25.1

